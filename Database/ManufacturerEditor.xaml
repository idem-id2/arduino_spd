<UserControl x:Class="HexEditor.Database.ManufacturerEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:HexEditor.Database"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    <UserControl.Resources>
        <!-- Ресурсы (если нужны) -->
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <Border Grid.Row="0" 
                Style="{StaticResource ToolbarPanelStyle}"
                Padding="8">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ Добавить" 
                        Click="AddButton_Click"
                        Style="{StaticResource CommandButtonStyle}"
                        Margin="0,0,4,0"/>
                <Button Content="❌ Удалить" 
                        Click="DeleteButton_Click"
                        Style="{StaticResource CommandButtonStyle}"
                        Margin="0,0,4,0"
                        x:Name="DeleteButton"/>
                <Separator Margin="8,0" Width="1" Background="{StaticResource SeparatorBrush}"/>
                <Button Content="⬆ Вверх" 
                        Click="MoveUpButton_Click"
                        Style="{StaticResource CommandButtonStyle}"
                        Margin="0,0,4,0"
                        x:Name="MoveUpButton"
                        ToolTip="Переместить выбранную строку вверх"/>
                <Button Content="⬇ Вниз" 
                        Click="MoveDownButton_Click"
                        Style="{StaticResource CommandButtonStyle}"
                        Margin="0,0,4,0"
                        x:Name="MoveDownButton"
                        ToolTip="Переместить выбранную строку вниз"/>
                <Separator Margin="8,0" Width="1" Background="{StaticResource SeparatorBrush}"/>
                <Button Content="💾 Сохранить" 
                        Click="SaveButton_Click"
                        Style="{StaticResource SuccessButtonStyle}"
                        Margin="0,0,4,0"/>
                <Button Content="🔄 Перезагрузить" 
                        Click="ReloadButton_Click"
                        Style="{StaticResource CommandButtonStyle}"
                        Margin="0,0,4,0"/>
                <Separator Margin="8,0" Width="1" Background="{StaticResource SeparatorBrush}"/>
                <TextBlock Text="База данных производителей (Manufacturers)" 
                           Style="{StaticResource CardTitleStyle}"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- DataGrid с записями -->
        <DataGrid x:Name="EntriesDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionChanged="EntriesDataGrid_SelectionChanged"
                  BeginningEdit="EntriesDataGrid_BeginningEdit"
                  Margin="8">
            <DataGrid.Columns>
                <!-- Нумерация строк (БАГ #7: используем свойство вместо AlternationIndex) -->
                <DataGridTextColumn Header="#" 
                                    Binding="{Binding RowNumber}"
                                    IsReadOnly="True"
                                    Width="50">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                            <Setter Property="FontWeight" Value="Normal"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- ID (Hex) -->
                <DataGridTextColumn Header="ID (Hex)" 
                                    Binding="{Binding IdHex, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"
                                    Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                    <DataGridTextColumn.EditingElementStyle>
                        <Style TargetType="TextBox">
                            <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
                            <Setter Property="CharacterCasing" Value="Upper"/>
                            <Setter Property="MaxLength" Value="4"/>
                            <EventSetter Event="PreviewTextInput" Handler="IdHexTextBox_PreviewTextInput"/>
                            <EventSetter Event="DataObject.Pasting" Handler="IdHexTextBox_Pasting"/>
                        </Style>
                    </DataGridTextColumn.EditingElementStyle>
                </DataGridTextColumn>
                
                <!-- Continuation Code (Read-only) -->
                <DataGridTextColumn Header="Continuation Code" 
                                    Binding="{Binding ContinuationCode, Mode=OneWay}"
                                    IsReadOnly="True"
                                    Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Manufacturer Code (Read-only) -->
                <DataGridTextColumn Header="Manufacturer Code" 
                                    Binding="{Binding ManufacturerCode, Mode=OneWay}"
                                    IsReadOnly="True"
                                    Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Название -->
                <DataGridTextColumn Header="Название" 
                                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                    Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Статусная строка -->
        <Border Grid.Row="2" 
                Style="{StaticResource StatusBarPanelStyle}"
                Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="StatusTextBlock" 
                           Text="Готов"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           FontSize="12"
                           Margin="4,0"/>
                <TextBlock Text=" | " 
                           Foreground="{StaticResource SeparatorBrush}"
                           Margin="4,0"/>
                <TextBlock x:Name="DatabasePathText" 
                           Text="База данных: загрузка..."
                           Foreground="{StaticResource MutedTextBrush}"
                           FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

