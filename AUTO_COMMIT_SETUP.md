# 🚀 Автоматические коммиты и push в GitHub

## ✅ Текущая настройка

### 1. **Автоматический коммит при сохранении файла** (VS Code/Cursor)
- **Расширение**: `emeraldwalk.RunOnSave` (уже в списке рекомендаций)
- **Триггер**: При сохранении файла (`Ctrl+S`)
- **Задержка**: 2 секунды (чтобы собрать несколько изменений)
- **Скрипт**: `.vscode/auto-commit.ps1`

### 2. **Автоматический push после коммита**
- **Hook**: `.git/hooks/post-commit.ps1`
- **Триггер**: После каждого коммита
- **Действие**: Автоматически отправляет изменения на GitHub

## 📦 Установка расширения

Если расширение `RunOnSave` ещё не установлено:

1. Откройте VS Code/Cursor
2. Нажмите `Ctrl+Shift+X` (Extensions)
3. Найдите: `RunOnSave` (автор: emeraldwalk)
4. Нажмите **Install**

Или через командную палитру:
- `Ctrl+Shift+P` → `Extensions: Install Extensions` → `RunOnSave`

## ⚙️ Как это работает

### Сценарий 1: Автоматический коммит при сохранении

1. Вы редактируете файл (например, `BaseSpdEditor.cs`)
2. Сохраняете файл (`Ctrl+S`)
3. **RunOnSave** запускает `.vscode/auto-commit.ps1`
4. Скрипт проверяет изменения и создаёт коммит
5. **Post-commit hook** автоматически отправляет на GitHub

### Сценарий 2: Ручной коммит

```powershell
# Вариант 1: Через скрипт
.\auto-commit.ps1

# Вариант 2: Через git
git add -A
git commit -m "Ваше сообщение"
# Post-commit hook автоматически отправит на GitHub
```

## 🔧 Настройки

### Файлы, которые автоматически коммитятся:
- `.cs` - C# файлы
- `.xaml` - XAML файлы
- `.json` - JSON файлы
- `.csproj`, `.sln` - файлы проекта
- `.md`, `.txt` - документация
- `.ps1` - PowerShell скрипты

### Файлы, которые игнорируются:
- `bin/`, `obj/` - скомпилированные файлы
- `.user`, `.suo` - пользовательские настройки
- `.git/` - файлы Git

### Задержка коммита:
- **2 секунды** после сохранения (чтобы собрать несколько изменений в один коммит)

## 🎯 Преимущества

1. **Полная автоматизация** - не нужно помнить о коммитах
2. **История изменений** - каждое сохранение = коммит
3. **Автоматический push** - изменения сразу на GitHub
4. **Безопасность** - только валидные файлы коммитятся

## ⚠️ Важные замечания

1. **Не коммитьте пароли и секреты** - они должны быть в `.gitignore`
2. **Проверяйте изменения** - перед важными изменениями просматривайте diff
3. **Делайте осмысленные коммиты** - автоматические коммиты имеют временные метки

## 🔍 Проверка работы

### Проверить, что расширение установлено:
```powershell
# В VS Code/Cursor: Ctrl+Shift+X → поиск "RunOnSave"
```

### Проверить, что hooks работают:
```powershell
cd E:\Projects\arduino_spd\arduino_spd
git status
# Если есть изменения, они будут автоматически закоммичены при сохранении
```

### Проверить последние коммиты:
```powershell
git log --oneline -5
git log --oneline origin/2025-12-10-10w9 -5
```

## 🛠️ Отключение автоматических коммитов

Если нужно временно отключить автоматические коммиты:

1. Откройте `.vscode/settings.json`
2. Закомментируйте или удалите секцию `emeraldwalk.runonsave`
3. Или установите `"runOn": "never"`

## 📝 Примеры сообщений коммитов

Автоматические коммиты имеют формат:
```
Auto-commit: 2025-12-11 14:16:13
Saved: BaseSpdEditor.cs

Изменения:
Modified: SpdDecoder/Editors/BaseSpdEditor.cs
Modified: SpdDecoder/Panels/SpdEditPanel.xaml.cs
```

---

**Готово!** Теперь каждый раз при сохранении файла изменения автоматически коммитятся и отправляются на GitHub! 🎉
