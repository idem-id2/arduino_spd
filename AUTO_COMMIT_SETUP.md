# 🔄 Автоматическая отправка изменений в Git

## ✅ Что настроено

### 1. Git Hook для автоматического push
- `.git/hooks/post-commit` - batch обертка для Windows (автоматически запускает PowerShell скрипт)
- `.git/hooks/post-commit.ps1` - PowerShell скрипт для автоматического push после коммита
- **Работает автоматически:** после каждого `git commit` выполняется `git push`

### 2. Скрипт автоматического коммита
- `.vscode/auto-commit.ps1` - скрипт для автоматического commit и push

### 3. Настройки VSCode/Cursor
- Автоматический push после commit включен
- Автоматическое сохранение файлов через 2 секунды

## 🚀 Как использовать

### Вариант 1: Ручной коммит (автоматический push)

1. Сохраните файлы (`Ctrl+S`)
2. Откройте Source Control (`Ctrl+Shift+G`)
3. Нажмите `+` для добавления файлов
4. Введите сообщение коммита
5. Нажмите `✓` для коммита
6. **Push выполнится автоматически** через:
   - Git hook `.git/hooks/post-commit` (после любого `git commit`)
   - Скрипт `.vscode/auto-commit.ps1` (при использовании RunOnSave)

### Вариант 2: Через задачу (полностью автоматически)

1. Нажмите `Ctrl+Shift+P`
2. Введите: `Tasks: Run Task`
3. Выберите: `Git: Auto Commit & Push`
4. Введите сообщение (или оставьте "Auto-save")
5. Все изменения будут автоматически закоммичены и отправлены

### Вариант 3: Через расширение RunOnSave (рекомендуется) ✅ АКТИВНО

1. Установите расширение **RunOnSave** (`emeraldwalk.RunOnSave`)
2. Перезапустите Cursor/VSCode
3. Теперь при каждом сохранении файла автоматически выполняется:
   - `git add [измененный файл]`
   - `git commit -m "Auto-save: [время] - [файл]"`
   - `git push origin [ветка]` ← **автоматически!**

## 📦 Установка расширения RunOnSave

1. Нажмите `Ctrl+Shift+X`
2. Найдите: `RunOnSave`
3. Установите: **Run on Save** от emeraldwalk
4. Перезапустите редактор

После установки расширения автоматические коммиты будут работать при каждом сохранении файла.

## ⚙️ Ручной запуск автоматического коммита

Если нужно вручную запустить автоматический коммит:

```powershell
# В терминале Cursor/VSCode
powershell -ExecutionPolicy Bypass -File .vscode/auto-commit.ps1
```

Или через задачу:
- `Ctrl+Shift+P` → `Tasks: Run Task` → `Git: Auto Save & Push`

## 🔍 Проверка работы

### Проверить Git hook:
```powershell
# Создать тестовый коммит
git commit --allow-empty -m "Test auto-push"
# Должен автоматически выполниться push
```

### Проверить автоматический коммит:
1. Измените любой файл
2. Сохраните (`Ctrl+S`)
3. Если установлено расширение RunOnSave - коммит создастся автоматически
4. Проверьте: `git log --oneline -1`

## 📊 История изменений

Все изменения автоматически сохраняются в Git с меткой времени:
- Формат коммита: `Auto-save: 2025-11-30 17:45:23`
- Каждое сохранение = отдельный коммит
- Все коммиты автоматически отправляются на GitHub

## ⚠️ Важные замечания

1. **Расширение RunOnSave** - рекомендуется для полной автоматизации
2. **Git hook** - работает всегда, автоматически push после каждого коммита
3. **Токен сохранен** - аутентификация выполняется автоматически
4. **История сохраняется** - все изменения в Git с временными метками

## 🛠️ Отключение автоматических коммитов

Если нужно временно отключить:

1. **Отключить RunOnSave:**
   - Закомментируйте секцию `emeraldwalk.RunOnSave` в `.vscode/settings.json`

2. **Отключить автоматический push:**
   - Переименуйте `.git/hooks/post-commit` в `.git/hooks/post-commit.disabled`

3. **Включить обратно:**
   - Верните настройки как было

---

**Готово!** Теперь все изменения автоматически сохраняются в Git и отправляются на GitHub. 🎉

