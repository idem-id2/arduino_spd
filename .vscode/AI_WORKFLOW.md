# 🤖 Рабочий процесс для работы с AI (нейросетью)

## 🎯 Настройка для максимального удобства

Система настроена для **прямой работы с `main` веткой** без создания Pull Request'ов. Это делает процесс максимально удобным для работы с AI-ассистентом.

## 🔄 Процесс работы

```
Сохранение файла (Ctrl+S)
    ↓
auto-commit.ps1 → git add + git commit
    ↓
post-commit hook → git push origin main
    ↓
Изменения сразу в main на GitHub ✅
```

**Никаких PR, никаких дополнительных шагов!**

## ✅ Преимущества для AI

1. **Простота** - один шаг: сохранение → коммит → push
2. **Скорость** - изменения сразу в main, без ожидания PR
3. **Прямой доступ** - AI видит все изменения сразу
4. **Меньше ошибок** - нет промежуточных шагов

## 📝 Как это работает

1. **Сохраняете файл** (`Ctrl+S`)
2. **Автоматически:**
   - `git add [файл]`
   - `git commit -m "Auto-save: [время] - [файл]"`
   - `git push origin main`
3. **Готово!** Изменения в main на GitHub

## 🔧 Текущая ветка

**Важно:** Убедитесь, что вы работаете в ветке `main`:

```bash
# Проверить текущую ветку
git branch --show-current

# Переключиться на main (если нужно)
git checkout main
```

## 🚫 Что отключено

- ❌ Автоматическое создание PR (не нужно для AI)
- ❌ Работа с feature-ветками (работаем только с main)

## 💡 Рекомендации

1. **Всегда работайте в `main`** - это упрощает процесс
2. **Сохраняйте часто** - каждое сохранение = автоматический коммит = точка восстановления
3. **Проверяйте логи** - в терминале видно все операции
4. **Используйте git для отката** - в случае ошибок AI можно легко откатить изменения

## 🔄 Откат изменений

**Важно:** Git используется для возможности отката любых изменений в случае сбоев или критических ошибок нейросети.

### Быстрый откат

```bash
# Откатить последний коммит
git reset --hard HEAD~1

# Откатить конкретный файл
git checkout HEAD -- path/to/file.cs

# Откатить до определенного времени
git log --since="1 hour ago" --oneline
git reset --hard <commit-hash>
```

**Подробное руководство:** См. `.vscode/ROLLBACK_GUIDE.md`

---

**Настроено для:** AI-ассистент (нейросеть)  
**Ветка:** `main`  
**PR:** Отключены (работа напрямую)  
**Откат:** Полностью поддерживается через git

