# 🔄 Руководство по откату изменений (Rollback Guide)

## 🎯 Назначение

Git используется для возможности **отката любых изменений** в случае сбоев или критических ошибок нейросети. Каждое сохранение файла автоматически создает коммит, что позволяет легко вернуться к любому предыдущему состоянию.

## ✅ Как это работает

### Автоматические коммиты

При каждом сохранении файла (`Ctrl+S`):
1. ✅ Файл автоматически добавляется в git (`git add`)
2. ✅ Создается коммит с временной меткой (`git commit`)
3. ✅ Изменения пушатся в GitHub (`git push`)
4. ✅ **Каждое изменение сохраняется в истории**

### Формат коммитов

```
Auto-save: 2025-12-10 14:23:45 - MainWindow.xaml.cs
Auto-save: 2025-12-10 14:24:12 - Arduino/Services/ArduinoService.cs
```

**Преимущества:**
- ✅ Временная метка для каждого изменения
- ✅ Имя файла в сообщении коммита
- ✅ Легко найти нужный момент для отката

---

## 🔙 Способы отката изменений

### 1. Откат последнего коммита (сохранить изменения в рабочей директории)

```bash
# Отменить последний коммит, но оставить изменения в файлах
git reset --soft HEAD~1

# Или отменить коммит и изменения в файлах
git reset --hard HEAD~1
```

### 2. Откат до конкретного коммита

```bash
# Найти нужный коммит
git log --oneline -20

# Откатиться до конкретного коммита (сохранить изменения)
git reset --soft <commit-hash>

# Откатиться до конкретного коммита (удалить изменения)
git reset --hard <commit-hash>
```

### 3. Откат конкретного файла

```bash
# Вернуть файл к состоянию последнего коммита
git checkout HEAD -- path/to/file.cs

# Вернуть файл к состоянию конкретного коммита
git checkout <commit-hash> -- path/to/file.cs
```

### 4. Откат до определенного времени

```bash
# Найти коммиты за определенный период
git log --since="2025-12-10 14:00" --until="2025-12-10 15:00" --oneline

# Откатиться к коммиту до определенного времени
git reset --hard <commit-hash-before-time>
```

### 5. Просмотр изменений перед откатом

```bash
# Посмотреть, что изменилось в последнем коммите
git show HEAD

# Посмотреть изменения в файле
git diff HEAD path/to/file.cs

# Посмотреть историю изменений файла
git log --oneline -- path/to/file.cs
```

---

## 🛠️ Практические примеры

### Пример 1: Нейросеть сломала код

```bash
# 1. Посмотреть последние коммиты
git log --oneline -10

# 2. Найти коммит до поломки (например, "Auto-save: 2025-12-10 14:20:00")
# 3. Откатиться к этому коммиту
git reset --hard <commit-hash>

# 4. Принудительно запушить изменения (если нужно)
git push origin main --force
```

### Пример 2: Откат только одного файла

```bash
# Нейросеть сломала MainWindow.xaml.cs, но остальные файлы в порядке

# 1. Найти последний рабочий коммит для этого файла
git log --oneline -- MainWindow.xaml.cs

# 2. Вернуть файл к рабочему состоянию
git checkout <working-commit-hash> -- MainWindow.xaml.cs

# 3. Закоммитить восстановление
git add MainWindow.xaml.cs
git commit -m "Rollback: Restore MainWindow.xaml.cs to working state"
```

### Пример 3: Откат всех изменений за последний час

```bash
# 1. Найти коммит час назад
git log --since="1 hour ago" --oneline

# 2. Найти коммит ДО начала проблемного периода
git log --until="1 hour ago" --oneline -1

# 3. Откатиться к этому коммиту
git reset --hard <commit-hash>

# 4. Запушить изменения
git push origin main --force
```

---

## 📊 Полезные команды для диагностики

### Просмотр истории

```bash
# Последние 20 коммитов с временем
git log --oneline --date=short --pretty=format:"%h %ad %s" -20

# Коммиты по конкретному файлу
git log --oneline -- path/to/file.cs

# Коммиты за сегодня
git log --since="today" --oneline

# Коммиты за последний час
git log --since="1 hour ago" --oneline
```

### Поиск проблемных изменений

```bash
# Найти коммиты с определенным текстом в сообщении
git log --grep="MainWindow" --oneline

# Найти коммиты, которые изменили определенную строку кода
git log -S "problematic code" --oneline

# Показать изменения в коммите
git show <commit-hash>
```

### Сравнение версий

```bash
# Сравнить текущее состояние с последним коммитом
git diff HEAD

# Сравнить два коммита
git diff <commit-hash-1> <commit-hash-2>

# Сравнить файл между коммитами
git diff <commit-hash-1> <commit-hash-2> -- path/to/file.cs
```

---

## ⚠️ Важные предупреждения

### `git reset --hard`

**ОСТОРОЖНО!** Эта команда **безвозвратно удаляет** все незакоммиченные изменения и откатывает коммиты.

**Перед использованием:**
1. ✅ Убедитесь, что все важные изменения закоммичены
2. ✅ Проверьте, что вы откатываетесь к правильному коммиту
3. ✅ Используйте `git log` для проверки истории

### `git push --force`

**ОСТОРОЖНО!** Эта команда перезаписывает историю на GitHub.

**Когда использовать:**
- После `git reset --hard` для синхронизации с GitHub
- Только если вы уверены, что хотите удалить коммиты навсегда

**Альтернатива (безопаснее):**
```bash
# Создать новый коммит, который отменяет изменения
git revert <commit-hash>
```

---

## 🔍 Стратегии отката

### Стратегия 1: Пошаговый откат

```bash
# Откатываться по одному коммиту за раз, проверяя результат
git reset --hard HEAD~1
# Проверить, работает ли код
# Если нет - откатиться еще на один коммит
git reset --hard HEAD~1
```

### Стратегия 2: Бинарный поиск

```bash
# 1. Найти диапазон коммитов (например, последние 10)
git log --oneline -10

# 2. Откатиться к середине диапазона
git reset --hard <middle-commit-hash>

# 3. Проверить, работает ли код
# 4. Если работает - проблема в более новых коммитах
# 5. Если не работает - проблема в более старых коммитах
# 6. Повторить для суженного диапазона
```

### Стратегия 3: Откат через revert (безопаснее)

```bash
# Создать новый коммит, который отменяет изменения
# (не перезаписывает историю)

# 1. Найти проблемный коммит
git log --oneline -10

# 2. Отменить его через revert
git revert <problematic-commit-hash>

# 3. Это создаст новый коммит, который отменяет изменения
# 4. История сохраняется, можно легко вернуться
```

---

## 📝 Рекомендации для работы с AI

### 1. Частые сохранения

✅ **Сохраняйте файлы часто** - каждое сохранение = новый коммит = точка восстановления

### 2. Проверка перед большими изменениями

```bash
# Перед большими изменениями - создать "чекпоинт"
git commit -m "Checkpoint: Before major refactoring"
```

### 3. Использование тегов для важных версий

```bash
# Пометить рабочую версию тегом
git tag -a v1.0-working -m "Stable working version"

# Откатиться к тегу
git checkout v1.0-working
```

### 4. Регулярная проверка статуса

```bash
# Проверить, что все изменения закоммичены
git status

# Посмотреть последние коммиты
git log --oneline -5
```

---

## 🚨 Экстренный откат

Если код полностью сломан и нужно быстро вернуться к рабочему состоянию:

```bash
# 1. Найти последний рабочий коммит (например, вчерашний)
git log --until="yesterday" --oneline -1

# 2. Откатиться к нему
git reset --hard <working-commit-hash>

# 3. Принудительно обновить GitHub
git push origin main --force

# 4. Проверить, что код работает
```

---

## 📚 Дополнительные ресурсы

- **Git документация:** https://git-scm.com/doc
- **Git reset:** `git help reset`
- **Git revert:** `git help revert`
- **Git log:** `git help log`

---

## 💡 Советы

1. **Всегда проверяйте `git log`** перед откатом
2. **Используйте `git show`** для просмотра изменений в коммите
3. **Делайте бэкапы** важных коммитов через теги
4. **Тестируйте код** после каждого отката
5. **Используйте `git revert`** вместо `git reset --hard` когда возможно

---

**Создано:** 2025-12-10  
**Версия:** 1.0  
**Статус:** ✅ Актуально

