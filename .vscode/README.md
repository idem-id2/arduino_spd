# ⚙️ Настройки VSCode/Cursor для проекта

## 🔄 Автоматические коммиты (RunOnSave)

Расширение **Run on Save** настроено для автоматических коммитов при сохранении файлов.

### Что происходит при сохранении:
1. ✅ Файл сохраняется
2. ✅ Автоматически выполняется `git add [файл]`
3. ✅ Автоматически создается коммит с сообщением: `Auto-save: [время] - [файл]`
4. ✅ Автоматически выполняется `git push` (через git hook)

### Какие файлы отслеживаются:
- ✅ `.cs` - C# файлы
- ✅ `.xaml` - XAML файлы
- ✅ `.json` - JSON файлы
- ✅ `.csproj`, `.sln` - файлы проектов
- ✅ `.md`, `.txt` - документация
- ✅ `.ino`, `.h` - Arduino файлы
- ✅ `.ps1`, `.psm1` - PowerShell скрипты

### Какие файлы игнорируются:
- ❌ `bin/`, `obj/` - папки сборки
- ❌ `Debug/`, `Release/` - папки сборки
- ❌ `.user`, `.suo` - пользовательские настройки
- ❌ `.vs/` - папка Visual Studio

### Проверка работы:

1. Откройте любой `.cs` файл
2. Внесите небольшое изменение (например, добавьте пробел)
3. Сохраните файл (`Ctrl+S`)
4. Проверьте вывод в терминале - должно появиться сообщение:
   ```
   ✓ Auto-committed: [путь к файлу] at [время]
   ```
5. Проверьте git log: `git log --oneline -1`

### Отключение автоматических коммитов:

Если нужно временно отключить:

1. Откройте `.vscode/settings.json`
2. Закомментируйте секцию `emeraldwalk.runonsave`:
   ```json
   // "emeraldwalk.runonsave": { ... }
   ```
3. Перезапустите Cursor/VSCode

### Ручной коммит:

Если автоматические коммиты отключены, можно коммитить вручную:
- Через Source Control панель (`Ctrl+Shift+G`)
- Через терминал: `git add . && git commit -m "message"`

---

**Настроено:** $(Get-Date -Format "yyyy-MM-dd HH:mm:ss")  
**Расширение:** Run on Save (emeraldwalk.RunOnSave)

