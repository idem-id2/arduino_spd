# 🔧 Настройка автоматической работы Git в Cursor/VSCode

## 📦 Установка расширений

### Обязательные расширения:

1. **GitLens** (`eamodio.gitlens`)
   - Расширенная работа с Git
   - История коммитов, blame, сравнение
   - Установка: `Ctrl+Shift+X` → поиск "GitLens" → Install

2. **Git Graph** (`mhutchie.git-graph`)
   - Визуализация истории Git
   - Установка: `Ctrl+Shift+X` → поиск "Git Graph" → Install

3. **Git History** (`donjayamanne.githistory`)
   - Просмотр истории файлов
   - Установка: `Ctrl+Shift+X` → поиск "Git History" → Install

### Опциональные расширения:

4. **GitHub Pull Requests** (`github.vscode-pull-request-github`)
   - Работа с Pull Requests прямо в редакторе

5. **C# Dev Kit** (`ms-dotnettools.csdevkit`)
   - Для C# проектов

## ⚙️ Настройки (уже созданы)

Файлы `.vscode/settings.json` и `.vscode/extensions.json` уже созданы с оптимальными настройками.

## 🚀 Использование

### Автоматический commit и push через палитру команд:

1. Нажмите `Ctrl+Shift+P` (или `F1`)
2. Введите: `Tasks: Run Task`
3. Выберите: `Git: Auto Commit & Push`
4. Введите сообщение коммита

### Быстрые команды:

- **Git: Commit Only** - только коммит без push
- **Git: Push** - отправить изменения
- **Git: Pull** - получить изменения
- **Git: Status** - статус репозитория

### Через боковую панель Git:

1. Откройте панель Source Control (`Ctrl+Shift+G`)
2. Нажмите `+` для добавления файлов
3. Введите сообщение коммита
4. Нажмите `✓` для коммита
5. Нажмите `...` → `Push` для отправки

### Автоматический push после коммита:

В настройках включено `"git.pushAfterCommit": true` - после каждого коммита автоматически выполняется push.

## 🔑 Настройка аутентификации GitHub

### Вариант 1: Personal Access Token

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo`
4. Скопируйте токен
5. При первом `git push` введите токен как пароль

### Вариант 2: GitHub Credential Manager

```powershell
# Установить GitHub Credential Manager
winget install GitHub.GitCredentialManager

# Или через Chocolatey
choco install git-credential-manager
```

После установки при первом `git push` откроется браузер для авторизации.

### Вариант 3: SSH ключи

1. Создайте SSH ключ:
```powershell
ssh-keygen -t ed25519 -C "your.email@example.com"
```

2. Скопируйте публичный ключ:
```powershell
cat ~/.ssh/id_ed25519.pub
```

3. GitHub → Settings → SSH and GPG keys → New SSH key
4. Вставьте ключ

5. Измените remote URL:
```powershell
git remote set-url origin git@github.com:YOUR_USERNAME/arduino_spd.git
```

## ⌨️ Горячие клавиши

- `Ctrl+Shift+G` - открыть панель Source Control
- `Ctrl+Enter` - быстрый коммит (если файлы добавлены)
- `Ctrl+Shift+P` → `Git: Push` - отправить изменения
- `Ctrl+Shift+P` → `Git: Pull` - получить изменения

## 🔄 Автоматизация через Git Hooks

Для полной автоматизации создайте `.git/hooks/post-commit`:

```powershell
# Создайте файл .git/hooks/post-commit (Windows)
@echo off
git push origin main
```

Или для PowerShell (`.git/hooks/post-commit.ps1`):

```powershell
git push origin main
```

Теперь после каждого коммита автоматически выполняется push.

## 📝 Рекомендуемый workflow

1. **Работа с кодом** - редактируйте файлы
2. **Сохранение** - `Ctrl+S` (автоматическое форматирование)
3. **Коммит** - `Ctrl+Shift+G` → `+` → сообщение → `✓`
4. **Push** - автоматически (если включен) или `Ctrl+Shift+P` → `Git: Push`

## ⚠️ Важные замечания

1. **Не коммитьте пароли и секреты** - они в `.gitignore`
2. **Делайте осмысленные коммиты** - описание должно быть понятным
3. **Перед push делайте pull** - если работаете на нескольких компьютерах
4. **Проверяйте изменения** - перед коммитом просматривайте diff

## 🆘 Решение проблем

### Ошибка: "Git: command not found"
- Убедитесь, что Git установлен: `git --version`
- Добавьте Git в PATH

### Ошибка аутентификации
- Проверьте Personal Access Token
- Или настройте SSH ключи
- Или используйте GitHub Credential Manager

### Ошибка: "failed to push"
```powershell
# Получите изменения с GitHub
git pull origin main --rebase

# Затем попробуйте снова
git push
```

---

**Готово!** Теперь Git работает автоматически в Cursor/VSCode. 🎉

