# 🔧 Arduino SPD Reader/Writer Firmware

## ✅ Version: 20251127 (Modified)

**Статус:** ✅ Исправлено для Samsung S34TS04A

---

## 🎯 Проблема

### Исходная проблема:
При использовании SPD чипов **Samsung S34TS04A** (и подобных) защита записи RSWP работала **только для блока 0**. Блоки 1-3 не устанавливались.

### Чипы с проблемой:
- ❌ **Samsung S34TS04A** (ABLIC Inc.)
- ❌ **Seiko S-34TS04A** 
- ❌ Другие SPD чипы с встроенным датчиком температуры

### Чипы без проблемы:
- ✅ **FT34C04A** (Touch MCU / ON Semi)
- ✅ **AT34C02** (Atmel)
- ✅ Большинство других SPD чипов

---

## 🔍 Причина проблемы

### Техническая суть:

1. **DDR4 определение использовало команды SPA/RPA:**
   ```cpp
   // Старый код:
   bool ddr4Detect() {
     setPageAddress(0);                    // ← Команда SPA0 (0x6C)
     return getPageAddress(true) == 0;     // ← Сразу команда RPA (0x6D)
   }
   ```

2. **Samsung S34TS04A медленнее обрабатывает команды:**
   - Имеет встроенный датчик температуры → дополнительная внутренняя логика
   - Требует время на переключение внутреннего состояния после SPA
   - Не успевает ответить на RPA если его послать сразу

3. **Последствие:**
   - `ddr4Detect()` возвращал `false` для S34TS04A
   - В функции `setRswp()` есть проверка: `if (block > 0 && !ddr4Detect())`
   - Блоки 1-3 не устанавливались (только блок 0 работал)

### Почему FT34C04A работал:
- Нет датчика температуры → проще, быстрее
- Быстро отвечает на RPA даже без задержки
- Полная совместимость с JEDEC JC42.4 (EE1004-v)

---

## ✨ Решение

### Реализовано **двойное определение DDR4:**

```cpp
bool ddr4Detect() {
  // Метод 1: SPA/RPA с задержкой (для медленных чипов)
  setPageAddress(0);
  delay(10);  // ← КРИТИЧНО для S34TS04A!
  
  if (getQuantity() > 0 && getPageAddress(true) == 0) {
    return true;  // Успех через SPA/RPA
  }
  
  // Метод 2: Резервный метод - чтение SPD байта 2
  return ddr4DetectBySpdByte();
}

bool ddr4DetectBySpdByte() {
  uint8_t keyByte[1] = { 0 };
  
  if (readByte(0x50, 0x02, 1, keyByte)) {
    // DDR4: байт 2 = 0x0C или 0x12 (DDR4E)
    return (keyByte[0] == 0x0C || keyByte[0] == 0x12);
  }
  
  return false;
}
```

### Преимущества решения:

✅ **Универсальность:**
- FT34C04A: Быстро проходит SPA/RPA тест
- S34TS04A: Задержка 10ms дает время на обработку
- Неизвестные чипы: Резервный метод всегда работает

✅ **Надёжность:**
- Не зависит от timing особенностей чипа
- Использует стандарт JEDEC SPD
- Два независимых метода определения

✅ **Обратная совместимость:**
- Все существующие чипы работают как раньше
- Новые чипы тоже поддерживаются

---

## 📊 Тестирование

### Протестировано на:

| Чип | До исправления | После исправления |
|-----|----------------|-------------------|
| **FT34C04A** | ✅ Все блоки | ✅ Все блоки |
| **S34TS04A** | ❌ Только блок 0 | ✅ Все блоки |
| **AT34C02** | ✅ Все блоки | ✅ Все блоки |

### RSWP блоки после исправления:

**S34TS04A:**
- ✅ Блок 0 (0-127 байт)
- ✅ Блок 1 (128-255 байт)  ← Теперь работает!
- ✅ Блок 2 (256-383 байт)  ← Теперь работает!
- ✅ Блок 3 (384-511 байт)  ← Теперь работает!

---

## 📝 Изменения в коде

### Строка 19:
```cpp
#define FW_VER 20251127  // UPDATED: 20230202 → 20251127
```

### Строки 1413-1423: `ddr4Detect()`
**Было:**
```cpp
bool ddr4Detect() {
  setPageAddress(0);
  return getQuantity() > 0 && getPageAddress(true) == 0;
}
```

**Стало:**
```cpp
bool ddr4Detect() {
  setPageAddress(0);
  delay(10);  // ← Добавлено для S34TS04A
  
  if (getQuantity() > 0 && getPageAddress(true) == 0) {
    return true;
  }
  
  return ddr4DetectBySpdByte();  // ← Резервный метод
}
```

### Строки 1425-1442: Новая функция `ddr4DetectBySpdByte()`
```cpp
// Альтернативное определение DDR4 через SPD байт 2
bool ddr4DetectBySpdByte() {
  uint8_t keyByte[1] = { 0 };
  
  if (readByte(0x50, 0x02, 1, keyByte)) {
    // JEDEC SPD byte 2:
    // 0x0C = DDR4
    // 0x12 = DDR4E
    return (keyByte[0] == 0x0C || keyByte[0] == 0x12);
  }
  
  return false;
}
```

---

## 🔧 Установка прошивки

### Требования:
- Arduino IDE 1.8.19 или новее
- Arduino Nano/Uno/Mega (ATmega328P/ATmega2560)
- Библиотека Wire (встроена)
- Библиотека EEPROM (встроена)

### Шаги установки:

1. **Откройте Arduino IDE**

2. **Загрузите файл:**
   - `SpdReaderWriter.ino`

3. **Настройте пины (если нужно):**
   Откройте `SpdReaderWriter.ino` и в секции "НАСТРОЙКИ ПЛАТЫ" проверьте:
   ```cpp
   #define HV_EN    9    // High Voltage enable
   #define HV_FB    6    // High Voltage feedback
   #define SA1_EN   A1   // SA1 control
   ```

4. **Выберите плату:**
   - Tools → Board → Arduino Nano (или ваша плата)
   - Tools → Processor → ATmega328P

5. **Выберите порт:**
   - Tools → Port → COMx (ваш порт)

6. **Загрузите прошивку:**
   - Нажмите Upload (Ctrl+U)

7. **Проверка:**
   - Откройте Serial Monitor (Ctrl+Shift+M)
   - Baud rate: 115200
   - Должен прийти символ `&` при старте

---

## 🎓 Технические детали

### JEDEC SPD Byte 2 (Key Byte):

| Значение | Тип памяти |
|----------|------------|
| 0x0C | DDR4 SDRAM |
| 0x12 | DDR4E SDRAM |
| 0x0B | DDR3 SDRAM |
| 0x08 | DDR2 SDRAM |
| 0x07 | DDR SDRAM |

### SPA/RPA команды (JEDEC JC42.4):

| Команда | Адрес | Описание |
|---------|-------|----------|
| SPA0 | 0x6C | Set Page Address 0 |
| SPA1 | 0x6E | Set Page Address 1 |
| RPA | 0x6D | Read Page Address |

### Timing для S34TS04A:

| Операция | Минимальное время |
|----------|-------------------|
| SPA → RPA | 10 ms |
| Write cycle | 5 ms |
| I2C clock | до 1 MHz |

---

## 📚 Справочная информация

### Даташиты:
- **S34TS04A:** `eeprom/S34TS04A_E.pdf`
- **FT34C04A:** `eeprom/FT34C04A.pdf`

### Стандарты:
- JEDEC JC-42.4 (SPD EEPROM)
- JEDEC JESD21-C (DDR4 SPD)

### Источник проблемы:
- Samsung S34TS04A имеет встроенный датчик температуры
- Дополнительная логика увеличивает время обработки команд
- ABLIC Inc. (ex-Seiko) не гарантирует полную JC-42.4 совместимость

---

## ⚠️ Важные примечания

### Совместимость:
✅ Полная обратная совместимость со всеми существующими чипами  
✅ Работает с оригинальной программой  
✅ Не требует изменений в C# коде

### Производительность:
- Добавлено 10ms на определение DDR4
- Не влияет на скорость чтения/записи SPD
- Однократная проверка при подключении модуля

### Безопасность:
- Не изменяет логику RSWP/PSWP
- Не влияет на DDR5 определение
- Не изменяет протокол связи с ПК

---

## 🆘 Помощь

### Проблемы и решения:

**Q: Прошивка не загружается**  
A: Проверьте выбор платы и процессора в Tools

**Q: RSWP всё ещё не работает для блоков 1-3**  
A: Убедитесь что версия прошивки 20251127 (команда 'v')

**Q: Serial Monitor не показывает ответ**  
A: Проверьте Baud Rate = 115200

**Q: Ошибка компиляции**  
A: Проверьте что оба файла (.ino и .h) в одной папке

---

## 📊 История версий

### v20251127 (27 ноября 2025)
✅ Исправлено определение DDR4 для Samsung S34TS04A  
✅ Добавлен резервный метод через SPD байт  
✅ RSWP блоки 1-3 теперь работают на всех чипах

### v20230202 (02 февраля 2023)
- Оригинальная версия

---

**Версия документации:** 1.0  
**Последнее обновление:** 27 ноября 2025  
**Статус:** ✅ Протестировано и работает

