# 📤 Инструкция по настройке GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name:** `arduino_spd` (или другое имя)
   - **Description:** `Arduino SPD Reader/Writer - WPF приложение для работы с SPD данными памяти DDR4/DDR5`
   - **Visibility:** Выберите **Public** или **Private**
   - **НЕ** создавайте README, .gitignore или лицензию (они уже есть)
4. Нажмите **"Create repository"**

## Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните команды:

```powershell
# Добавьте remote репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/arduino_spd.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Если используете SSH:**
```powershell
git remote add origin git@github.com:YOUR_USERNAME/arduino_spd.git
git branch -M main
git push -u origin main
```

## Шаг 3: Настройте синхронизацию (автоматический push)

### Вариант 1: Push при каждом коммите (рекомендуется)

Создайте alias для удобного push:

```powershell
# Настройте глобальный alias
git config --global alias.save "!git add -A && git commit -m 'Auto-save' && git push"

# Теперь можно использовать:
git save
```

### Вариант 2: Ручной push (стандартный)

После каждого изменения:

```powershell
# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push
```

### Вариант 3: Автоматический push через Git Hook

Создайте файл `.git/hooks/post-commit` (Windows):

```powershell
# Создайте файл .git/hooks/post-commit
@echo off
git push origin main
```

Или для PowerShell создайте `.git/hooks/post-commit.ps1`:

```powershell
git push origin main
```

## Шаг 4: Настройте Git пользователя (если еще не настроено)

```powershell
# Глобальная настройка (для всех репозиториев)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Или только для этого репозитория
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

## Шаг 5: Проверьте подключение

```powershell
# Проверить remote репозиторий
git remote -v

# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline -5
```

## 🔄 Ежедневная работа с репозиторием

### Стандартный workflow:

```powershell
# 1. Проверить изменения
git status

# 2. Добавить все изменения
git add .

# 3. Создать коммит с описанием
git commit -m "Описание ваших изменений"

# 4. Отправить на GitHub
git push
```

### Если работаете на нескольких компьютерах:

```powershell
# Перед началом работы - получить последние изменения
git pull

# После работы - отправить изменения
git add .
git commit -m "Описание изменений"
git push
```

## 🔐 Настройка аутентификации GitHub

### Вариант 1: Personal Access Token (рекомендуется)

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. При `git push` используйте токен как пароль

### Вариант 2: SSH ключи

1. Создайте SSH ключ:
```powershell
ssh-keygen -t ed25519 -C "your.email@example.com"
```

2. Скопируйте публичный ключ:
```powershell
cat ~/.ssh/id_ed25519.pub
```

3. GitHub → Settings → SSH and GPG keys → New SSH key
4. Вставьте ключ и сохраните

5. Используйте SSH URL:
```powershell
git remote set-url origin git@github.com:YOUR_USERNAME/arduino_spd.git
```

## 📝 Полезные команды

```powershell
# Посмотреть изменения перед коммитом
git diff

# Отменить изменения в файле (до git add)
git checkout -- filename

# Отменить git add (но сохранить изменения)
git reset HEAD filename

# Посмотреть историю
git log --oneline --graph --all

# Создать новую ветку
git checkout -b feature-name

# Переключиться на ветку
git checkout main

# Слить ветку в main
git checkout main
git merge feature-name
```

## ⚠️ Важные замечания

1. **Не коммитьте пароли и секретные ключи** - они уже в .gitignore
2. **Делайте осмысленные коммиты** - описание должно быть понятным
3. **Регулярно делайте push** - не накапливайте изменения
4. **Перед push делайте pull** - если работаете на нескольких компьютерах

## 🆘 Решение проблем

### Ошибка: "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/arduino_spd.git
```

### Ошибка: "failed to push some refs"
```powershell
# Получите изменения с GitHub
git pull origin main --rebase

# Затем попробуйте снова
git push
```

### Ошибка аутентификации
- Проверьте Personal Access Token
- Или настройте SSH ключи
- Или используйте GitHub Desktop для упрощения

---

**Готово!** Теперь ваш проект синхронизирован с GitHub. 🎉

