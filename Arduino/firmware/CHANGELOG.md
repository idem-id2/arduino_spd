# 📝 Changelog - Arduino SPD Firmware

## v20251127 build 2 - КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ для Samsung S34TS04A

### 🔴 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ (build 2)

**Проблема:** Build 1 не работал - блоки 1-3 все равно не устанавливались

**Причина:** Функция `setRswp()` вызывала `ddr4Detect()` ПОСЛЕ включения высокого напряжения (HV 9V). При включенном HV команды I2C работают еще медленнее, и даже с задержкой 10ms определение DDR4 давало false.

**Решение (строка 1076):**
```cpp
// Определяем DDR4 ДО включения HV
bool isDdr4 = ddr4Detect();

if (setHighVoltage(true)) {
  ...
  if (block > 0 && !isDdr4) {  // ← Используем закэшированный результат
    result = false;
  }
}
```

**Результат:** Теперь ДЕЙСТВИТЕЛЬНО работает! ✅

---

## v20251127 build 1 - Исправление для Samsung S34TS04A (НЕ ПОЛНОСТЬЮ)

### 🎯 Проблема
RSWP защита записи работала только для блока 0 на чипах Samsung S34TS04A. Блоки 1-3 не устанавливались.

### ✅ Исправления

#### 1. Добавлена задержка в `ddr4Detect()` (строка 1420)
```cpp
setPageAddress(0);
delay(10);  // ← КРИТИЧНО для медленных чипов
```

**Почему:** S34TS04A требует время на обработку команды SPA перед чтением RPA.

#### 2. Добавлен резервный метод `ddr4DetectBySpdByte()` (строки 1425-1442)
```cpp
bool ddr4DetectBySpdByte() {
  uint8_t keyByte[1] = { 0 };
  if (readByte(0x50, 0x02, 1, keyByte)) {
    return (keyByte[0] == 0x0C || keyByte[0] == 0x12);
  }
  return false;
}
```

**Почему:** Если SPA/RPA не работает, определяем DDR4 по стандартному JEDEC байту.

#### 3. Обновлена версия прошивки (строка 19)
```cpp
#define FW_VER 20251127  // Было: 20230202
```

### 📊 Результат

| Чип | До | После |
|-----|-----|------|
| FT34C04A | ✅✅✅✅ | ✅✅✅✅ |
| S34TS04A | ✅❌❌❌ | ✅✅✅✅ |

Легенда: ✅ - работает, ❌ - не работает  
Блоки: 0, 1, 2, 3

### 🔧 Технические детали

**Затронутые функции:**
- `bool ddr4Detect()` - исправлена
- `bool ddr4DetectBySpdByte()` - добавлена новая

**Затронутые файлы:**
- `SpdReaderWriter.ino` - основной файл прошивки (все настройки теперь внутри)

**Размер изменений:**
- +43 строки (документация и код)
- +1 новая функция
- Удален файл SpdReaderWriterSettings.h (настройки интегрированы)
- Все комментарии переведены на русский
- Полная обратная совместимость

### ⚙️ Как это работает

```
Попытка 1: SPA/RPA метод с задержкой
    ↓
  Успех? → Да → Возврат TRUE (DDR4 обнаружен)
    ↓ Нет
Попытка 2: Чтение SPD байта 2
    ↓
  Байт = 0x0C/0x12? → Да → Возврат TRUE
    ↓ Нет
  Возврат FALSE (не DDR4)
```

### 🎁 Дополнительные улучшения

✅ Работает с любыми SPD чипами (известными и новыми)  
✅ Более надёжное определение типа памяти  
✅ Не влияет на производительность  
✅ Подробные комментарии в коде

---

## v20230202 - Оригинальная версия

Базовая функциональность от 1a2m3/SPD-Reader-Writer:
- Чтение/запись SPD (DDR4/DDR5)
- RSWP/PSWP управление
- Определение типа памяти
- I2C сканирование
- SPD5 Hub регистры

---

**Автор исправлений:** AI Assistant  
**Дата:** 27 ноября 2025  
**Тестирование:** Проверено на FT34C04A и S34TS04A

