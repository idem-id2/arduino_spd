# 🔍 Детальный анализ модуля SpdDecoder

**Дата анализа:** 27 ноября 2025  
**Версия модуля:** 2.0  
**Статус:** ✅ Production Ready

---

## 📋 Содержание

1. [Обзор модуля](#обзор-модуля)
2. [Архитектура](#архитектура)
3. [Компоненты](#компоненты)
4. [Поток данных](#поток-данных)
5. [Детали реализации](#детали-реализации)
6. [Интеграция с приложением](#интеграция-с-приложением)
7. [Особенности и паттерны](#особенности-и-паттерны)

---

## 🎯 Обзор модуля

**SpdDecoder** — модуль для декодирования и редактирования SPD (Serial Presence Detect) данных модулей памяти DDR3/DDR4/DDR5.

### Основные функции:
- ✅ Декодирование SPD данных в читаемый формат
- ✅ Редактирование SPD параметров через UI
- ✅ Поддержка DDR3/DDR4/DDR5
- ✅ CRC проверка и исправление
- ✅ Интерактивная подсветка байтов в HexEditor
- ✅ Валидация данных

### Статистика:
- **Строк кода:** ~5268
- **Файлов:** 14
- **Классов:** 12
- **Интерфейсов:** 2

---

## 🏗️ Архитектура

### Слоистая архитектура (Layered Architecture):

```
┌─────────────────────────────────────────────────────────┐
│  PRESENTATION LAYER (UI)                                │
│  ┌───────────────────────────────────────────────────┐  │
│  │  SpdInfoPanel (просмотр)                         │  │
│  │  SpdEditPanel (редактирование)                   │  │
│  └────────────────────────────┬──────────────────────┘  │
│                                │ использует               │
│                                ▼                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │  SpdParser (Facade Pattern)                     │  │
│  │  - Упрощенный API для UI                        │  │
│  │  - Координирует работу декодеров                │  │
│  └────────────────────────────┬──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────┐
│  APPLICATION LAYER (Бизнес-логика)                       │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Editors/                                        │  │
│  │  - BaseSpdEditor (базовая логика)               │  │
│  │  - Ddr4SpdEditor (30+ полей)                   │  │
│  │  - Ddr5SpdEditor (базовая реализация)          │  │
│  └────────────────────────────┬──────────────────────┘  │
│                                │ использует               │
│                                ▼                          │
│  ┌───────────────────────────────────────────────────┐  │
│  │  Decoders/                                       │  │
│  │  - BaseSpdDecoder (JEDEC базовая логика)        │  │
│  │  - Ddr4SpdDecoder (полное декодирование)        │  │
│  │  - Ddr5SpdDecoder (базовая реализация)          │  │
│  └────────────────────────────┬──────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────┐
│  CORE LAYER (Контракты и фабрики)                        │
│  ┌───────────────────────────────────────────────────┐  │
│  │  ISpdDecoder (интерфейс декодера)                │  │
│  │  ISpdEditor (интерфейс редактора)                │  │
│  │  SpdTypeDetector (Factory Pattern)              │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### Принципы проектирования:
- ✅ **Separation of Concerns** — каждый слой решает свою задачу
- ✅ **Dependency Inversion** — зависимость от абстракций (интерфейсов)
- ✅ **Open/Closed** — расширяемость через наследование
- ✅ **Single Responsibility** — один класс = одна задача

---

## 📦 Компоненты

### 1. Core Layer (4 файла)

#### `ISpdDecoder.cs` (15 строк)
**Назначение:** Интерфейс для декодеров SPD

```csharp
internal interface ISpdDecoder
{
    void Populate(
        List<SpdInfoPanel.InfoItem> moduleInfo,
        List<SpdInfoPanel.InfoItem> dramInfo,
        List<SpdInfoPanel.TimingRow> timingRows);
}
```

**Особенности:**
- Минималистичный интерфейс
- Метод `Populate` заполняет готовые списки (избегает создания объектов)

#### `ISpdEditor.cs` (70 строк)
**Назначение:** Интерфейс для редактирования SPD

```csharp
internal interface ISpdEditor
{
    void LoadData(byte[] data);
    List<EditField> GetEditFields();
    List<ByteChange> ApplyChanges(Dictionary<string, string> fieldValues);
    Dictionary<string, string> ValidateFields(Dictionary<string, string> fieldValues);
}
```

**Модели данных:**
- `EditField` — описание редактируемого поля (Id, Label, Value, Type, Category, ToolTip)
- `EditFieldType` — тип поля (TextBox, ComboBox, CheckBox, Numeric)
- `ComboBoxItem` — элемент выпадающего списка

#### `SpdTypeDetector.cs` (99 строк)
**Назначение:** Фабрика для создания декодеров/редакторов (Factory Pattern)

**Методы:**
- `DetectMemoryType(byte[] data)` — определение типа памяти по байту 2
- `CreateDecoder(byte[] data, ForcedMemoryType)` — создание декодера
- `CreateEditor(byte[] data, ForcedMemoryType)` — создание редактора

**Поддерживаемые типы:**
- `0x0C` → DDR4
- `0x12` → DDR5
- `0x0B` → DDR3 (fallback)

**Особенности:**
- Поддержка принудительного выбора типа (`ForcedMemoryType`)
- Возвращает `null` для неподдерживаемых типов

#### `SpdParser.cs` (125 строк)
**Назначение:** Упрощенный API для UI (Facade Pattern)

**Свойства:**
- `ModuleInfo` — информация о модуле памяти
- `DramInfo` — информация о DRAM чипах
- `TimingRows` — таблица таймингов

**Методы:**
- `Parse(ForcedMemoryType)` — парсинг SPD данных

**Особенности:**
- Автоматически определяет тип памяти
- Fallback на упрощенный парсинг для DDR3
- Обработка ошибок с безопасным fallback

---

### 2. Decoders Layer (3 файла)

#### `BaseSpdDecoder.cs` (362 строки)
**Назначение:** Базовая логика декодирования JEDEC стандарта

**Ключевые методы:**

1. **Работа с производителями:**
   - `GetManufacturerName(byte, byte)` — получение имени по JEDEC ID
   - Использует `ManufacturerDatabase` (JSON база данных)

2. **Работа с данными:**
   - `GetPartNumber(int start, int end)` — чтение ASCII строки
   - `GetSerialNumber(int start, int end)` — чтение серийного номера (ASCII или hex)
   - `FindSpecificPartNumber()` — поиск vendor-specific part number

3. **Работа с таймингами:**
   - `DecodeTimebases()` — декодирование MTB/FTB (Medium/Fine Timebase)
   - `GetTimebaseValue(int)` — чтение MTB значения
   - `GetFineTimebaseValue(int)` — чтение FTB значения (signed)
   - `GetCombinedTimebaseValue(int, int)` — комбинированное значение (MTB + FTB)
   - `GetTimingNsFromComposite(...)` — чтение composite таймингов (LSB + MSB фрагменты)

4. **Форматирование:**
   - `FormatDataSize(ulong)` — форматирование размера (B/KB/MB/GB/TB)
   - `GetManufacturingDateString(int, int)` — форматирование даты (BCD → "Week X, YYYY")

**Особенности:**
- Абстрактный класс — нельзя создать напрямую
- Защищенные методы для наследников
- Обработка edge cases (недостаточно данных, невалидные значения)

#### `Ddr4SpdDecoder.cs` (2160 строк)
**Назначение:** Полное декодирование DDR4 SPD по JEDEC стандарту

**Основные разделы:**

1. **Module Info (18 полей):**
   - Manufacturer (байты 320-321)
   - Part Number (байты 329-348)
   - Serial Number (байты 325-328)
   - Specific Part (байты 353-383)
   - JEDEC DIMM Label (составное поле)
   - Architecture (байт 3)
   - Speed Grade (байты 18, 125)
   - Capacity (байты 4-5, 12-13)
   - Organization (байты 12-13, 6, 4)
   - Thermal Sensor (байт 14)
   - Module Height/Thickness (байты 128-129)
   - Register Info (байты 131-134)
   - Revision/Raw Card (байты 130, 128, 142-143)
   - Address Mapping (байт 136)
   - Manufacturing Date/Location (байты 323-324, 322)
   - CRC (байты 126-127, 254-255)

2. **DRAM Info (14 полей):**
   - Manufacturer (байты 350-351)
   - DRAM Part Number (реконструкция из базы данных)
   - Package Type (байт 6)
   - Die Density/Count (байты 4-5, 6)
   - Composition (байты 4-5, 12)
   - Input Clock Frequency (байты 18, 125)
   - Addressing (байты 4-5)
   - Minimum Timing Delays (байты 18, 24-29, 120-123, 125)
   - Read Latencies (байты 20-23)
   - Supply Voltage (байт 11)
   - SPD Revision (байт 1)
   - XMP Info (байты 384+)

3. **Timing Table:**
   - JEDEC профиль (базовая частота)
   - XMP Profile 1 (если присутствует)
   - XMP Profile 2 (если присутствует)

**Специфичные методы DDR4:**

- `GetJedecLabel()` — формирование JEDEC DIMM Label
  - Формат: `{Capacity} {Org} PC4-{Speed}{Bin}-{Module}-{Revision}`
  - Пример: `16GB 2Rx8 PC4-3200AA-UA0-10`

- `GetDramPartNumber()` — реконструкция DRAM part number
  - Использует базу данных `dram_part_numbers.json`
  - Сопоставление по: manufacturer, die density, device width, die count, package type

- `GetDieTypeInfo()` — определение типа die (B-die, C-die, E-die, etc.)
  - Анализ part number по паттернам
  - Поддержка Samsung, Hynix, Micron

- `GetDdr4CrcInfo()` — проверка CRC
  - Block 0: байты 0-125, CRC в 126-127
  - Block 1: байты 128-253, CRC в 254-255
  - Алгоритм: CRC-16-CCITT (полином 0x1021)

- `FixCrc(byte[] buffer)` — исправление CRC
  - Пересчитывает и записывает правильные значения

**Особенности:**
- Обработка всех edge cases (Multi Load Stack, 3DS, Monolithic)
- Поддержка XMP профилей
- Детальное логирование для отладки

#### `Ddr5SpdDecoder.cs` (1824 строки)
**Назначение:** Декодирование DDR5 SPD по JEDEC JESD400-5C

**Основные отличия от DDR4:**

1. **Размер SPD:** 1024 байта (vs 512 для DDR4)
2. **CRC:** Один блок (байты 0-509, CRC в 510-511)
3. **Тайминги:** 16-bit значения в picoseconds (vs 8-bit MTB/FTB для DDR4)
4. **SPD Hub:** Дополнительная информация о SPD Hub устройстве
5. **Channels:** Поддержка нескольких каналов (1, 2, 4, 8)
6. **Sub-channels:** DDR5 использует 2 sub-channel на канал

**Реализованные поля:**

**Module Info:**
- Manufacturer (байты 512-513)
- Part Number (байты 521-550, 30 байт)
- Serial Number (байты 517-520)
- Manufacturing Date/Location (байты 515-516, 514)
- JEDEC DIMM Label (составное поле)
- Module Type (байт 3)
- Speed Grade (байты 20-21, 235)
- Capacity (байты 4, 12-13, 235)
- Organization (байты 12-13, 235)
- Thermal Sensor (байт 14)
- Module Height/Thickness (байты 229-230)
- SPD Revision (байт 1)
- CRC (байты 510-511)
- **SPD Hub Device Info:**
  - Hub Device Type
  - Hub Manufacturer
  - Hub Model
  - Temperature Sensor Status
  - Write Protection Status

**DRAM Info:**
- Manufacturer (байты 552-553)
- Package Type (байт 6)
- Die Density (байты 4-5)
- Addressing (байты 4-5)
- Input Clock Frequency (байты 20-21, 235)
- Minimum Timings (байты 30-39)
- Read Latencies (байты 22-31, 80 бит для CL22-CL101)
- CAS Write Latencies (байты 32-35, 32 бит для CWL22-CWL53)
- Supply Voltage (байт 11, Vdd/Vddq + Vpp)
- **Extended Timings:**
  - tRFC1, tRFC2, tRFCsb (Refresh Cycle Times)
  - tFAW (Four Activate Window)
  - tRTP (Read to Precharge)
  - tWR (Write Recovery Time)
- Refresh Management (байт 9)
- PMIC Info (байты 551, 554-560)

**Timing Table:**
- JEDEC профиль (базовая частота)
- TODO: XMP 3.0 / EXPO профили

**Специфичные методы DDR5:**

- `GetTckNs()` — чтение tCK из 16-bit значения (байты 20-21)
  - Формат: LSB (байт 20), MSB (байт 21)
  - Значение в picoseconds, конвертируется в nanoseconds

- `GetTiming16bitNs(int lsb, int msb)` — чтение 16-bit таймингов
  - Все основные тайминги DDR5 используют 16-bit формат

- `GetModuleCapacityBytes()` — расчет емкости модуля
  - Учитывает: density, die count, device width, bus width, ranks, channels
  - Формула: `(DensityPerDie × DieCount / 8) × (BusWidth / DeviceWidth) × Ranks × Channels`

- `GetDdr5CrcInfo()` — проверка CRC
  - Один блок: байты 0-509, CRC в 510-511
  - Алгоритм: CRC-16-CCITT (тот же, что для DDR4)

- `FixCrc(byte[] buffer)` — исправление CRC

**Статус реализации:**
- ✅ Базовое декодирование реализовано
- ✅ CRC проверка/исправление работает
- ⏳ XMP 3.0 / EXPO профили — TODO
- ⏳ Полная поддержка всех extended timings — частично

---

### 3. Editors Layer (3 файла)

#### `BaseSpdEditor.cs` (534 строки)
**Назначение:** Базовая логика редактирования SPD

**Ключевые методы:**

1. **Создание общих полей:**
   - `CreateCommonFields()` — создает поля, общие для всех типов SPD
     - Module Manufacturer (ComboBox с JEDEC ID)
     - Module Part Number (TextBox, ASCII)
     - Module Serial Number (TextBox, hex или ASCII)
     - Manufacturing Date (Year/Week, BCD)
     - Manufacturing Location (TextBox, hex)
     - Module Type (ComboBox)
     - SPD Revision (Major/Minor, hex)
     - Memory Type (ComboBox, read-only)

2. **Применение общих изменений:**
   - `ApplyCommonChanges(Dictionary<string, string>)` — применяет изменения общих полей
   - Обрабатывает: Manufacturer ID, Part Number, Serial Number, Date, Location, Type, Revision

3. **Вспомогательные методы:**
   - `ReadAsciiString(int, int)` — чтение ASCII строки
   - `ReadSerialNumber(int, int)` — чтение серийного номера (ASCII или hex)
   - `WriteAsciiString(byte[], int, int, string)` — запись ASCII строки
   - `WriteSerialNumber(byte[], int, int, string)` — запись серийного номера
   - `TryParseHex(string, out byte)` — парсинг hex строки
   - `TryParseBcd(string, out byte)` — парсинг BCD значения

**Особенности:**
- Работает напрямую с `byte[]` данными (не зависит от декодеров)
- Валидация на уровне парсинга (TryParse методы)
- Поддержка различных форматов (ASCII, hex, BCD)

#### `Ddr4SpdEditor.cs` (852 строки)
**Назначение:** Редактирование DDR4 SPD параметров

**Реализованные категории полей:**

1. **MemoryModule (общие + специфичные):**
   - Module Manufacturer
   - Module Part Number
   - Module Serial Number
   - Manufacturing Date (Year/Week)
   - Manufacturing Location
   - Module Type
   - SPD Revision

2. **DramComponents:**
   - DRAM Manufacturer (ComboBox)
   - Package Type (Monolithic checkbox + Die Count)

3. **Timing (14 полей):**
   - tCK (MTB + FTB)
   - tAA / CAS Latency (MTB + FTB)
   - tRCD (MTB + FTB)
   - tRP (MTB + FTB)
   - tRAS (composite: LSB + MSB фрагменты)
   - tRC (composite + FTB)
   - tFAW (composite)
   - tRRD_S (MTB + FTB)
   - tRRD_L (MTB + FTB)
   - tWR (composite)
   - tWTR_S (composite)

4. **ModuleConfig:**
   - Number of Ranks (байт 12, биты 5-3)
   - Device Width (байт 12, биты 2-0)
   - Primary Bus Width (байт 13, биты 2-0)
   - ECC Support (байт 13, бит 3)
   - Thermal Sensor (байт 14, бит 7)
   - Supply Voltage Operable (байт 11, бит 0)

**Методы:**

- `GetEditFields()` — создает список редактируемых полей
  - Группирует по категориям
  - Устанавливает ToolTip с описанием байтов
  - Устанавливает MaxLength для TextBox

- `ApplyChanges(Dictionary<string, string>)` — применяет изменения
  - Валидирует значения
  - Обновляет соответствующие байты
  - Обрабатывает composite байты (tRAS, tRC, tFAW, tWR, tWTR_S)
  - Возвращает список изменений (`ByteChange[]`)

- `ValidateFields(Dictionary<string, string>)` — валидация полей
  - Проверяет BCD формат (Year/Week)
  - Проверяет числовые диапазоны (Die Count: 0-7, Ranks: 1-8)
  - Возвращает словарь ошибок

**Особенности:**
- Обработка composite байтов (несколько полей в одном байте)
- Поддержка signed значений (FTB для таймингов)
- Валидация на уровне редактора

#### `Ddr5SpdEditor.cs` (571 строка)
**Назначение:** Редактирование DDR5 SPD параметров

**Реализованные категории:**

1. **MemoryModule:**
   - Module Manufacturer (байты 512-513)
   - Module Part Number (байты 521-550, 30 байт)
   - Module Serial Number (байты 517-520)
   - Manufacturing Date (байты 515-516)

2. **DramComponents:**
   - DRAM Manufacturer (байты 552-553)

3. **Timing:**
   - tCK (байты 20, 235) — **ВНИМАНИЕ:** В DDR5 tCK хранится в 16-bit формате (байты 20-21), но редактор использует старый формат
   - tAA (байты 20, 235) — аналогично
   - tRCD (байты 21, 236)
   - tRP (байты 25, 240)
   - tRAS (байты 26, 241)

4. **ModuleConfig:**
   - Number of Ranks (байт 12, биты 5-3)
   - Device Width (байт 12, биты 2-0)
   - Primary Bus Width (байт 13, биты 2-0)
   - Thermal Sensor (байт 14, бит 7)

**Статус реализации:**
- ✅ Базовые поля реализованы
- ⚠️ **ПРОБЛЕМА:** tCK и tAA используют неправильный формат (8-bit вместо 16-bit)
- ⏳ Extended timings — не реализованы
- ⏳ PMIC информация — не реализована

**Известные проблемы:**
- tCK должен читаться из байтов 20-21 (16-bit), но редактор использует байт 20 + байт 235 (FTB)
- Это несоответствие с декодером, который правильно читает 16-bit значения

---

### 4. Panels Layer (4 файла)

#### `SpdInfoPanel.xaml.cs` (378 строк)
**Назначение:** WPF UserControl для отображения декодированной информации

**Основные компоненты:**

1. **Структуры данных:**
   - `InfoItem` — элемент информации
     - Label, Value, IsHighlighted
     - ByteOffset, ByteLength (одиночный диапазон)
     - ByteRanges (множественные диапазоны)
   - `TimingRow` — строка таблицы таймингов (TODO)

2. **События:**
   - `HighlightBytes` — выделение байтов в HexEditor
   - `ClearHighlight` — снятие выделения

3. **Методы:**
   - `UpdateSpdData(byte[], ForcedMemoryType)` — обновление данных
     - Создает `SpdParser`
     - Парсит данные
     - Строит динамический UI
   - `Clear()` — очистка панели
   - `BuildDynamicUI()` — построение UI из списков InfoItem
   - `CreateCategorySection(string, List<InfoItem>)` — создание секции категории

**UI особенности:**

- **Динамическое построение:** UI создается программно из списков InfoItem
- **Zebra-полосы:** Чередование цветов строк (четные/нечетные)
- **Интерактивность:** При наведении мыши на строку подсвечиваются соответствующие байты в HexEditor
- **Секции:** Группировка по категориям (MEMORY MODULE, DRAM COMPONENTS)
- **Кэширование ресурсов:** Стили и кисти кэшируются для оптимизации

**Оптимизации:**

- Кэширование стилей и кистей (`CacheResources()`)
- Использование `for` вместо `foreach` для избежания O(n²) сложности
- Debouncing для событий подсветки (через таймеры в MainWindow)

#### `SpdInfoPanel.xaml` (40 строк)
**Назначение:** XAML разметка для панели просмотра

**Структура:**
- `Grid` с 2 строками
- `ScrollViewer` с `StackPanel` для динамических секций
- `Button` "Copy All Data" внизу

**Особенности:**
- Минималистичная разметка (UI строится программно)
- Использование стилей из ресурсов (`CommandButtonStyle`)

#### `SpdEditPanel.xaml.cs` (485 строк)
**Назначение:** WPF UserControl для редактирования SPD параметров

**Основные компоненты:**

1. **Структуры данных:**
   - `ByteChange` — изменение байта
     - Offset, NewData[]

2. **События:**
   - `ChangesApplied` — событие применения изменений
     - Передает список `ByteChange[]`

3. **Методы:**
   - `LoadSpdData(byte[], ForcedMemoryType)` — загрузка данных для редактирования
     - Создает копию данных
     - Создает редактор через `SpdTypeDetector`
     - Строит динамический UI
   - `Clear()` — очистка панели
   - `BuildDynamicUI()` — построение UI из EditField[]
   - `CreateCategorySection(string, List<EditField>)` — создание секции категории
   - `CreateFieldControl(EditField)` — создание контрола для поля
   - `CollectFieldValues()` — сбор значений из UI
   - `ApplyButton_Click()` — обработка кнопки Apply
   - `ResetButton_Click()` — сброс изменений

**UI особенности:**

- **Динамическое построение:** UI создается из списка `EditField`
- **Группировка по категориям:** MemoryModule, DramComponents, Timing, ModuleConfig
- **Типы контролов:**
  - TextBox — для текстовых и числовых полей
  - ComboBox — для выбора из списка (Manufacturer, Module Type)
  - CheckBox — для булевых значений
- **Валидация:** Проверка значений перед применением
- **Кнопки:** Apply Changes, Reset

**Оптимизации:**

- Кэширование контролов в `_dynamicFieldControls` Dictionary
- Использование стилей из ресурсов
- Минимальная перерисовка при обновлении

#### `SpdEditPanel.xaml` (50 строк)
**Назначение:** XAML разметка для панели редактирования

**Структура:**
- `Grid` с 2 строками
- `ScrollViewer` с `StackPanel` для динамических полей
- `StackPanel` с кнопками Apply/Reset внизу

**Стили:**
- `SuccessButtonStyle` — для кнопки Apply
- `CommandButtonStyle` — для кнопки Reset

---

## 🔄 Поток данных

### Сценарий 1: Просмотр SPD данных

```
1. MainWindow.xaml.cs
   └─> UpdateSpdInfo()
       └─> SpdInfoPanel.UpdateSpdData(data, forcedType)

2. SpdInfoPanel.UpdateSpdData()
   └─> new SpdParser(data)
       └─> parser.Parse(forcedType)

3. SpdParser.Parse()
   └─> SpdTypeDetector.CreateDecoder(data, forcedType)
       └─> new Ddr4SpdDecoder(data) или new Ddr5SpdDecoder(data)

4. Decoder.Populate(moduleInfo, dramInfo, timingRows)
   └─> PopulateModuleInfo(moduleInfo)
   └─> PopulateDramInfo(dramInfo)
   └─> PopulateTimings(timingRows)

5. SpdInfoPanel.BuildDynamicUI()
   └─> CreateCategorySection("MEMORY MODULE", moduleInfo)
   └─> CreateCategorySection("DRAM COMPONENTS", dramInfo)
   └─> Добавление обработчиков MouseEnter/MouseLeave для подсветки
```

### Сценарий 2: Редактирование SPD данных

```
1. MainWindow.xaml.cs
   └─> UpdateSpdEditPanel()
       └─> SpdEditPanel.LoadSpdData(data, forcedType)

2. SpdEditPanel.LoadSpdData()
   └─> SpdTypeDetector.CreateEditor(data, forcedType)
       └─> new Ddr4SpdEditor() или new Ddr5SpdEditor()
   └─> editor.LoadData(data)
   └─> BuildDynamicUI()

3. SpdEditPanel.BuildDynamicUI()
   └─> editor.GetEditFields()
   └─> Группировка по категориям
   └─> CreateCategorySection() для каждой категории
   └─> CreateFieldControl() для каждого поля

4. Пользователь редактирует поля → нажимает "Apply Changes"

5. SpdEditPanel.ApplyButton_Click()
   └─> CollectFieldValues() — сбор значений из UI
   └─> editor.ValidateFields(fieldValues) — валидация
   └─> editor.ApplyChanges(fieldValues) — применение изменений
       └─> Обновление байтов в _currentSpdData
       └─> Возврат списка ByteChange[]

6. SpdEditPanel.ChangesApplied?.Invoke(changes)

7. MainWindow.OnSpdEditChangesApplied(changes)
   └─> HexEditor.ReplaceData(offset, newData) для каждого изменения
   └─> ScheduleSpdInfoUpdate() — обновление панели просмотра
```

### Сценарий 3: Подсветка байтов в HexEditor

```
1. Пользователь наводит мышь на строку в SpdInfoPanel

2. SpdInfoPanel.MouseEnter (на rowGrid)
   └─> HighlightBytes?.Invoke(ranges)

3. MainWindow.OnSpdInfoHighlightBytes(ranges)
   └─> Debouncing через DispatcherTimer (10ms)
   └─> ApplyHighlight()
       └─> HexEditor.AddBookmarkRange(offset, length, Colors.Red)

4. Пользователь убирает мышь

5. SpdInfoPanel.MouseLeave
   └─> ClearHighlight?.Invoke()

6. MainWindow.OnSpdInfoClearHighlight()
   └─> Debouncing через DispatcherTimer (50ms)
   └─> ApplyClearHighlight()
       └─> HexEditor.RemoveBookmarkRange(offset, Colors.Red)
```

---

## 🔧 Детали реализации

### 1. Декодирование таймингов

#### DDR4 (8-bit MTB + 8-bit FTB):
```csharp
// Пример: tAA (CAS Latency)
// Байт 24: MTB (unsigned, 0-255)
// Байт 123: FTB (signed, -128 to +127)

double mtb = GetTimebaseValue(24);        // MTB * 125 ps
double ftb = GetFineTimebaseValue(123);   // FTB * 1 ps (signed)
double taaNs = (mtb + ftb) / 1000.0;      // Конвертация в nanoseconds
```

#### DDR5 (16-bit в picoseconds):
```csharp
// Пример: tAA
// Байты 30-31: 16-bit значение в picoseconds (LSB, MSB)

byte lsb = Data[30];
byte msb = Data[31];
int taa_ps = (msb << 8) | lsb;
double taaNs = taa_ps / 1000.0;  // Конвертация в nanoseconds
```

#### Composite тайминги (DDR4):
```csharp
// Пример: tRAS
// Байт 28: LSB (8 бит)
// Байт 27, биты 3-0: MSB фрагмент (4 бита)
// Формула: value = (MSB_fragment << 8) | LSB_fragment

int lsb = Data[28];
int msbFragment = (Data[27] >> 0) & 0x0F;  // Биты 3-0
int combinedValue = (msbFragment << 8) | lsb;
double trasNs = combinedValue * MediumTimebasePs / 1000.0;
```

### 2. CRC вычисление

**Алгоритм:** CRC-16-CCITT (полином 0x1021)

```csharp
private ushort ComputeDdr4Crc(int start, int length)
{
    const ushort polynomial = 0x1021;
    ushort crc = 0;

    for (int i = start; i < start + length; i++)
    {
        crc ^= (ushort)(Data[i] << 8);
        for (int bit = 0; bit < 8; bit++)
        {
            crc = (crc & 0x8000) != 0
                ? (ushort)((crc << 1) ^ polynomial)
                : (ushort)(crc << 1);
        }
    }

    return crc;
}
```

**Использование:**
- DDR4: 2 блока (0-125 @ 126-127, 128-253 @ 254-255)
- DDR5: 1 блок (0-509 @ 510-511)

### 3. Реконструкция DRAM Part Number

**Проблема:** JEDEC не хранит DRAM part number в SPD

**Решение:** Реконструкция из базы данных по параметрам

```csharp
private string ReconstructDramPartNumberFromSpdParams()
{
    // Получаем параметры из SPD
    string manufacturer = GetManufacturerName(Data[350], Data[351]);
    int dieDensityMb = GetTotalCapacityPerDieMb();
    int deviceWidth = GetDeviceWidthBits();
    int dieCount = GetDieCount();
    bool isMultiLoadStack = !IsMonolithic() && GetSignalLoading() == 1;

    // Ищем в базе данных
    var entries = DramPartNumberDatabase.LoadDatabase();
    foreach (var entry in entries)
    {
        if (MatchesSpdParams(entry, manufacturer, dieDensityMb, 
                           deviceWidth, dieCount, isMultiLoadStack))
        {
            return entry.PartNumber;
        }
    }

    return "—";
}
```

**База данных:** `Database/dram_part_numbers.json`

### 4. Формирование JEDEC DIMM Label

**Формат DDR4:** `{Capacity} {Org} PC4-{Speed}{Bin}-{Module}-{Revision}`

**Пример:** `16GB 2Rx8 PC4-3200AA-UA0-10`

**Компоненты:**
- Capacity: `FormatCapacityLabel()` — "16GB"
- Organization: `FormatRankDescriptor()` — "2Rx8"
- Speed: `BuildSpeedCode()` — "3200AA" (data rate + speed bin suffix)
- Module: `BuildModuleSection()` — "UA0" (type + raw card)
- Revision: `GetSpdRevisionSuffix()` — "10" (major.minor)

**Формат DDR5:** `{Capacity} {Org} PC5-{Number}{Bin}-{Arch}-{Height}-{Attr}`

**Пример:** `16GB 1Rx8 PC5-5600B-UA0-1010-XT`

**Компоненты:**
- Capacity: "16GB"
- Organization: "1Rx8"
- PC5 Number: data rate (5600)
- Speed Bin: "B", "C", "A" (из байта 7)
- Architecture: "UA0" (Module Type + Revision + Channel)
- Height/Thickness: "1010" (hex коды из байтов 229-230)
- Attributes: "XT" (Extended + Thermal sensor)

### 5. Обработка composite байтов

**Проблема:** Некоторые тайминги хранятся в нескольких байтах с перекрытием

**Пример:** Байт 27 используется для tRAS и tRC:
- Байт 27, биты 3-0: tRAS MSB
- Байт 27, биты 7-4: tRC MSB
- Байт 28: tRAS LSB
- Байт 29: tRC LSB

**Решение в редакторе:**
```csharp
// При применении tRAS
byte newByte28 = (byte)(trasValue & 0xFF);
byte byte27 = Data[27];
byte newByte27 = (byte)((byte27 & 0xF0) | ((trasValue >> 8) & 0x0F));
// Сохраняем биты 7-4 (tRC) и обновляем биты 3-0 (tRAS)
```

---

## 🔗 Интеграция с приложением

### Использование в MainWindow.xaml.cs

**Инициализация:**
```csharp
// Подписка на события
SpdInfoPanel.HighlightBytes += OnSpdInfoHighlightBytes;
SpdInfoPanel.ClearHighlight += OnSpdInfoClearHighlight;
SpdEditPanel.ChangesApplied += OnSpdEditChangesApplied;
```

**Обновление данных:**
```csharp
// При загрузке файла или изменении данных
private void UpdateSpdInfo()
{
    var data = HexEditor.ReadBytes(0, (int)Math.Min(HexEditor.DocumentLength, SpdConstants.DDR5_SPD_SIZE));
    var forcedType = GetForcedMemoryType();
    SpdInfoPanel.UpdateSpdData(data, forcedType);
}

private void UpdateSpdEditPanel()
{
    var data = HexEditor.ReadBytes(0, (int)Math.Min(HexEditor.DocumentLength, SpdConstants.DDR5_SPD_SIZE));
    var forcedType = GetForcedMemoryType();
    SpdEditPanel.LoadSpdData(data, forcedType);
}
```

**Обработка изменений:**
```csharp
private void OnSpdEditChangesApplied(List<SpdEditPanel.ByteChange> changes)
{
    foreach (var change in changes)
    {
        HexEditor.ReplaceData(change.Offset, change.NewData);
    }
    ScheduleSpdInfoUpdate(immediate: true);
}
```

**CRC исправление:**
```csharp
private void FixCrcButton_Click(object sender, RoutedEventArgs e)
{
    var buffer = HexEditor.ReadBytes(0, length);
    
    if (_lastSpdIsDdr4)
    {
        var decoder = new Ddr4SpdDecoder(buffer);
        changed = decoder.FixCrc(buffer);
    }
    else if (_lastSpdIsDdr5)
    {
        var decoder = new Ddr5SpdDecoder(buffer);
        changed = decoder.FixCrc(buffer);
    }
    
    if (changed)
    {
        HexEditor.ReplaceData(offset, newData);
    }
}
```

### Использование в MainWindow.xaml

```xml
<!-- SpdInfoPanel для просмотра -->
<local:SpdInfoPanel x:Name="SpdInfoPanel" 
                    Grid.Column="1"
                    Grid.Row="0"/>

<!-- SpdEditPanel для редактирования -->
<local:SpdEditPanel x:Name="SpdEditPanel"
                     Grid.Column="1"
                     Grid.Row="1"/>
```

---

## 🎓 Особенности и паттерны

### Применённые паттерны:

1. **Strategy Pattern** — `ISpdDecoder` с реализациями DDR4/DDR5
   - Позволяет легко добавлять новые типы памяти

2. **Factory Pattern** — `SpdTypeDetector`
   - Централизованное создание декодеров/редакторов
   - Автоматическое определение типа памяти

3. **Facade Pattern** — `SpdParser`
   - Упрощенный API для UI
   - Скрывает сложность работы с декодерами

4. **Template Method Pattern** — `BaseSpdDecoder` / `BaseSpdEditor`
   - Общая логика в базовом классе
   - Специфичная логика в наследниках

5. **Observer Pattern** — события в Panels
   - `HighlightBytes` / `ClearHighlight`
   - `ChangesApplied`

### Оптимизации:

1. **Кэширование ресурсов:**
   - Стили и кисти кэшируются в `SpdInfoPanel`
   - Избегает повторных поисков в ресурсах

2. **Debouncing событий:**
   - Подсветка байтов через таймеры (10ms/50ms)
   - Предотвращает мерцание при быстром движении мыши

3. **Ленивая инициализация:**
   - UI строится только при необходимости
   - Очистка при отсутствии данных

4. **Минимальные перерисовки:**
   - Обновление только измененных элементов
   - Использование `for` вместо `foreach` где возможно

### Обработка ошибок:

1. **Безопасное чтение:**
   - Проверка длины данных перед доступом
   - Fallback значения для невалидных данных

2. **TryParse паттерны:**
   - `TryParseHex()`, `TryParseBcd()`
   - Возвращают `false` при ошибке

3. **Обработка исключений:**
   - Try-catch блоки в критических местах
   - Логирование ошибок в Debug

### Известные ограничения:

1. **DDR5 редактор:**
   - ⚠️ tCK и tAA используют неправильный формат (8-bit вместо 16-bit)
   - ⏳ Extended timings не реализованы
   - ⏳ PMIC информация не редактируется

2. **Timing Table:**
   - ⏳ Таблица таймингов в UI не реализована (TODO в коде)

3. **XMP 3.0 / EXPO:**
   - ⏳ Профили XMP 3.0 для DDR5 не реализованы

4. **DDR3:**
   - ⚠️ Только упрощенный fallback парсинг
   - ⏳ Полный декодер не реализован

---

## 📊 Метрики качества

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| **Архитектура** | 9/10 | Отличная слоистая структура, четкое разделение ответственности |
| **Код качество** | 8.5/10 | Хорошая читаемость, но есть большие файлы (Ddr4SpdDecoder: 2160 строк) |
| **Тестируемость** | 7/10 | Интерфейсы позволяют тестировать, но нет unit-тестов |
| **Документация** | 9/10 | Отличная документация в README, комментарии в коде |
| **Расширяемость** | 9/10 | Легко добавить новые типы памяти через Strategy Pattern |
| **Производительность** | 8/10 | Хорошие оптимизации, но можно улучшить кэширование |

**Общая оценка:** 8.5/10 ⭐⭐⭐⭐⭐⭐⭐⭐⭐⚪

---

## 🚀 Рекомендации по улучшению

### Приоритет 1: Исправить DDR5 редактор
- Использовать 16-bit формат для tCK и tAA
- Синхронизировать с декодером

### Приоритет 2: Реализовать Timing Table в UI
- Добавить DataGrid для отображения таймингов
- Поддержка JEDEC + XMP профилей

### Приоритет 3: Разбить большие файлы
- Вынести логику модуля в `Ddr4ModuleInfoDecoder`
- Вынести логику DRAM в `Ddr4DramInfoDecoder`
- Вынести логику таймингов в `Ddr4TimingDecoder`

### Приоритет 4: Unit-тесты
- Тесты для декодеров
- Тесты для редакторов
- Тесты для CRC вычислений

---

## 📚 Ссылки

- **JEDEC DDR4 SPD:** JESD21-C (DDR4 SDRAM SPD)
- **JEDEC DDR5 SPD:** JESD400-5C (DDR5 SDRAM SPD)
- **JEP106:** Manufacturer ID Codes
- **README модуля:** `SpdDecoder/README.md`

---

**Модуль:** SpdDecoder  
**Версия:** 2.0  
**Статус:** ✅ Production Ready (с ограничениями для DDR5)  
**Оценка:** 8.5/10 ⭐⭐⭐⭐⭐⭐⭐⭐⭐⚪

