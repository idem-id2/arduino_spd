<Window x:Class="HexEditor.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Height="180"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource AppWindowBackground}">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="PromptTextBlock"
                   Grid.Row="0"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource PrimaryTextBrush}"
                   FontSize="13"
                   Text="{Binding Prompt, RelativeSource={RelativeSource AncestorType=Window}}"/>
        
        <TextBox x:Name="InputTextBox"
                 Grid.Row="2"
                 MaxLength="{Binding MaxLength, RelativeSource={RelativeSource AncestorType=Window}}"
                 Text="{Binding DefaultValue, RelativeSource={RelativeSource AncestorType=Window}}"
                 KeyDown="InputTextBox_KeyDown"
                 Style="{StaticResource EditFieldValueStyle}"/>
        
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="75"
                    MinHeight="30"
                    Margin="0,0,10,0"
                    Click="OkButton_Click"
                    IsDefault="True"
                    Style="{StaticResource SuccessButtonStyle}"/>
            <Button Content="Отмена"
                    Width="75"
                    MinHeight="30"
                    Click="CancelButton_Click"
                    IsCancel="True"
                    Style="{StaticResource CommandButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>

