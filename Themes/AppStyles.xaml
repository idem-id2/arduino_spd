<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
    
    <!-- ========================================= -->
    <!-- Ultra Light Modern Color Palette -->
    <!-- Inspired by: VS Code Light, Notion, GitHub -->
    <!-- ========================================= -->
    
    <!-- Background Colors - Very Light & Clean -->
    <SolidColorBrush x:Key="AppWindowBackground" Color="#FAFBFC"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SurfaceHoverBrush" Color="#F6F8FA"/>
    <SolidColorBrush x:Key="SurfaceBorderBrush" Color="#E1E4E8"/>
    
    <!-- Primary Colors - Modern Blue -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#0969DA"/>
    <SolidColorBrush x:Key="PrimaryHoverBrush" Color="#0860CA"/>
    <SolidColorBrush x:Key="PrimaryPressedBrush" Color="#0550AE"/>
    
    <!-- Text Colors - High Contrast for Readability -->
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#24292F"/>
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#57606A"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#6E7781"/>
    <SolidColorBrush x:Key="PlaceholderTextBrush" Color="#8C959F"/>
    
    <!-- Accent Colors -->
    <SolidColorBrush x:Key="SeparatorBrush" Color="#D0D7DE"/>
    <SolidColorBrush x:Key="AccentBrush" Color="#0969DA"/>
    
    <!-- Zebra Stripes (for tables/lists) -->
    <SolidColorBrush x:Key="ZebraEvenBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ZebraOddBrush" Color="#F6F8FA"/>
    
    <!-- State Colors - Modern, Vibrant -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#1A7F37"/>
    <SolidColorBrush x:Key="SuccessLightBrush" Color="#DFF6DD"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#CF222E"/>
    <SolidColorBrush x:Key="ErrorLightBrush" Color="#FFEBE9"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#BF8700"/>
    <SolidColorBrush x:Key="WarningLightBrush" Color="#FFF8C5"/>
    <SolidColorBrush x:Key="InfoBrush" Color="#0969DA"/>
    <SolidColorBrush x:Key="InfoLightBrush" Color="#DDF4FF"/>
    
    <!-- Button State Colors - Ultra Clean -->
    <SolidColorBrush x:Key="ButtonBackground" Color="Transparent"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#F3F4F6"/>
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="#E5E7EB"/>
    <SolidColorBrush x:Key="ButtonBorderBrush" Color="#D0D7DE"/>
    
    <!-- Success Button Colors -->
    <SolidColorBrush x:Key="SuccessButtonBackground" Color="#1A7F37"/>
    <SolidColorBrush x:Key="SuccessButtonHoverBackground" Color="#166F2E"/>
    <SolidColorBrush x:Key="SuccessButtonPressedBackground" Color="#125F26"/>
    
    <!-- ========================================= -->
    <!-- Common Metrics -->
    <!-- ========================================= -->
    
    <Thickness x:Key="CardPadding">20</Thickness>
    <Thickness x:Key="CardMargin">0,0,0,0</Thickness>
    <Thickness x:Key="RowMargin">0,2,0,2</Thickness>
    <sys:Double x:Key="RowMinHeight">32</sys:Double>
    
    <!-- ========================================= -->
    <!-- Card Styles - Ultra Flat, Sharp Corners -->
    <!-- ========================================= -->
    
    <Style TargetType="Border" x:Key="CardStyle">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="0"/>
        <Setter Property="Padding" Value="{StaticResource CardPadding}"/>
        <Setter Property="Margin" Value="{StaticResource CardMargin}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
    </Style>
    
    <Style TargetType="Border" x:Key="SectionBlockStyle">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="20"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- Typography - Clean & Readable -->
    <!-- ========================================= -->
    
    <!-- FontFamily для всего приложения: JetBrains Mono NL с fallback на Consolas, Courier New, monospace -->
    <!-- Загружается программно в App.xaml.cs, здесь используется как ресурс -->
    <FontFamily x:Key="ApplicationFontFamily">Consolas, 'Courier New', monospace</FontFamily>
    
    <!-- Стиль по умолчанию для TextBlock - использует ApplicationFontFamily -->
    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
    </Style>
    
    <!-- Стиль по умолчанию для TextBox - использует ApplicationFontFamily -->
    <Style TargetType="TextBox">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
    </Style>
    
    <!-- Стиль по умолчанию для Label - использует ApplicationFontFamily -->
    <Style TargetType="Label">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="CardTitleStyle">
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Margin" Value="0,0,0,16"/>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="FieldLabelText">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
    </Style>
    
    <Style TargetType="TextBlock" x:Key="FieldValueText">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="FontWeight" Value="Regular"/>
    </Style>
    
    <!-- Стиль для TextBox label (позволяет выделение текста) -->
    <Style TargetType="TextBox" x:Key="EditFieldLabelStyle">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,2,12,2"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="IsReadOnlyCaretVisible" Value="False"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
        <!-- TextTrimming не поддерживается в TextBox, используем TextWrapping="NoWrap" -->
        <Setter Property="TextWrapping" Value="NoWrap"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- Button Styles - Ultra Flat, Sharp Corners -->
    <!-- ========================================= -->
    
    <Style TargetType="Button" x:Key="CommandButtonStyle">
        <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            CornerRadius="0"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"
                                        RecognizesAccessKey="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonPressedBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryPressedBrush}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Success Button - Filled Style -->
    <Style TargetType="Button" x:Key="SuccessButtonStyle" BasedOn="{StaticResource CommandButtonStyle}">
        <Setter Property="Background" Value="{StaticResource SuccessButtonBackground}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessButtonBackground}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SuccessButtonHoverBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SuccessButtonHoverBackground}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="{StaticResource SuccessButtonPressedBackground}"/>
                <Setter Property="BorderBrush" Value="{StaticResource SuccessButtonPressedBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- ========================================= -->
    <!-- Input Field Styles - Clean, Sharp -->
    <!-- ========================================= -->
    
    <Style TargetType="TextBox" x:Key="EditFieldValueStyle">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                    Padding="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                <!-- BorderThickness остается 1 при фокусе (не делаем жирный бордюр) -->
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                <Setter Property="Opacity" Value="0.6"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style TargetType="TextBox" x:Key="EditFieldValueStyleError" BasedOn="{StaticResource EditFieldValueStyle}">
        <Setter Property="Background" Value="{StaticResource ErrorLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>
    
    <Style TargetType="TextBox" x:Key="SpdInfoLabelStyle">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="0,2,12,2"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="IsTabStop" Value="False"/>
    </Style>
    
    <Style TargetType="TextBox" x:Key="SpdInfoValueStyle">
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="IsReadOnly" Value="True"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- ComboBox Style - Modern, Sharp -->
    <!-- ========================================= -->
    
    <Style TargetType="ComboBox" x:Key="ComboBoxStyle">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="28"/>
                            </Grid.ColumnDefinitions>

                            <ToggleButton x:Name="ToggleButton"
                                          Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"/>

                            <ContentPresenter Grid.Column="0"
                                              Margin="10,0,4,0"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="Stretch"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                              IsHitTestVisible="False"/>

                            <Path Grid.Column="1"
                                  x:Name="Arrow"
                                  Data="M 0 0 L 4 4 L 8 0 Z"
                                  Fill="{StaticResource SecondaryTextBrush}"
                                  Width="8"
                                  Height="4"
                                  Stretch="Uniform"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   MinWidth="{TemplateBinding ActualWidth}"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Background="{StaticResource SurfaceBrush}"
                                        BorderBrush="{StaticResource SurfaceBorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="0"
                                        Margin="0,2,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer Margin="2"
                                                  CanContentScroll="True">
                                        <StackPanel IsItemsHost="True"
                                                    KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Border" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Default ComboBox style -->
    <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBoxStyle}">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
    </Style>
    
    <Style TargetType="ComboBox" x:Key="EditFieldComboBoxStyle" BasedOn="{StaticResource ComboBoxStyle}"/>
    
    <!-- ========================================= -->
    <!-- CheckBox Style - Modern, Sharp -->
    <!-- ========================================= -->
    
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- Panel Styles - Ultra Clean -->
    <!-- ========================================= -->
    
    <Style TargetType="Border" x:Key="ToolbarPanelStyle">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    
    <Style TargetType="Border" x:Key="StatusBarPanelStyle">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="BorderThickness" Value="0,1,0,0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    
    <Style TargetType="Border" x:Key="BadgeStyle">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="0"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="0,0,12,0"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- TabControl & TabItem Styles - Modern, Clean -->
    <!-- ========================================= -->
    
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource AppWindowBackground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Padding" Value="0"/>
    </Style>
    
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{StaticResource SecondaryTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid>
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            RecognizesAccessKey="True"/>
                        </Border>
                        <!-- Нижний индикатор для выбранной вкладки -->
                        <Border x:Name="Indicator"
                                Height="2"
                                Background="Transparent"
                                VerticalAlignment="Bottom"
                                HorizontalAlignment="Stretch"
                                Margin="0"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource PrimaryBrush}"/>
                            <Setter TargetName="ContentSite" Property="TextElement.FontWeight" Value="SemiBold"/>
                            <Setter TargetName="Indicator" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Border" Property="Background" Value="Transparent"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource PrimaryBrush}"/>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ContentSite" Property="Opacity" Value="0.4"/>
                            <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource MutedTextBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- ========================================= -->
    <!-- Menu Styles - Modern -->
    <!-- ========================================= -->
    
    <Style TargetType="Menu">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    
    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontSize" Value="13"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{StaticResource ButtonHoverBackground}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- ========================================= -->
    <!-- ListBox Styles - Clean -->
    <!-- ========================================= -->
    
    <Style TargetType="ListBox">
        <Setter Property="FontFamily" Value="{DynamicResource ApplicationFontFamily}"/>
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- DataGrid Styles - Modern -->
    <!-- ========================================= -->
    
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SurfaceBorderBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
        <Setter Property="RowBackground" Value="{StaticResource ZebraEvenBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource ZebraOddBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- ProgressBar Style - Modern -->
    <!-- ========================================= -->
    
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Height" Value="4"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- ScrollBar Styles - Minimal -->
    <!-- ========================================= -->
    
    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource SurfaceHoverBrush}"/>
    </Style>
    
    <!-- ========================================= -->
    <!-- Separator Style - Clean -->
    <!-- ========================================= -->
    
    <Style TargetType="Separator">
        <Setter Property="Background" Value="{StaticResource SeparatorBrush}"/>
        <Setter Property="Height" Value="1"/>
    </Style>
    
</ResourceDictionary>
